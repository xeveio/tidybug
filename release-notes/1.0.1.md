A performance update.
- The Monitor tab now uses about 12% of one CPU core instead of up to 90%. It no longer redraws continuously: the live indicator and the per-second numbers stopped animating, and the charts draw faster.
- The notch overlay and the app's panels no longer spend CPU on invisible shadows.
- System commands that time out, used by Clean and Optimize, can no longer hang.
